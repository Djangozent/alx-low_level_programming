#include "main.h"
/**
 * _islower - checks for lowrcase character
 * @c: the character to check
 * Return: 1 if i is lowercase, otherwise 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
